// Package peg contains the in-memory representation of PEG grammars, the
// compiler entry points and model nodes used by the ogopego parser generator.
// This package implements grammar linking, validation, left-recursion support,
// and model transformations required to turn grammar descriptions into
// executable parse behavior.
package peg
