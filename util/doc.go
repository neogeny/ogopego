// Package util collects small helper functions and packages used across the
// project (string helpers, newline detection, pyre abstraction, etc.).
// These helpers are intentionally minimal; where appropriate they are kept in
// subpackages to maintain a clear API surface.
package util
