package context

import (
	"github.com/neogeny/ogopego/config"
	"github.com/neogeny/ogopego/input"
)

// Cfg is an alias for config.Cfg.
type Cfg = config.Cfg

// Cursor is an alias for input.Cursor.
type Cursor = input.Cursor

// Configurable is an alias for config.Configurable.
type Configurable = config.Configurable
